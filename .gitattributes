# Normalize line endings to LF everywhere (matches Prettier's default and CI on Linux).
* text=auto eol=lf

# Lockfile is generated — collapse it in PR diffs.
pnpm-lock.yaml linguist-generated=true -diff
